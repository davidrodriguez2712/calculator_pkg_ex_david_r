[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "calculator_pkg_ex_david_r"
version = "0.2.0"
dependencies = [
    "tqdm~=4.7"
]

[project.scripts]
calculator_pkg_ex = "calculator_pkg_ex_david_r.__main__:main"

[project.optional-dependencies]
test = [
    "pytest==9.1.1",
    "mypy==2.3.1",
    "ruff==0.16.7",
    "pre-commit==4.6.2",
    "tox==4.61.4"
]

